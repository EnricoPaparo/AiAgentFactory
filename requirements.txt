# AISA — AISolutionArchitect
# Installazione: pip install -r requirements.txt
# Python 3.11+ richiesto

anthropic>=0.40.0    # client Claude
pypdf>=4.0.0         # lettura PDF
python-docx>=1.0.0   # lettura Word
weasyprint>=60.0     # generazione PDF
